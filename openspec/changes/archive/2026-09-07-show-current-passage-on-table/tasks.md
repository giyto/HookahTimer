## 1. Индикатор проходки

- [x] 1.1 Добавить локализованные ресурсы компактного индикатора и развёрнутого доступного описания, а также стабильный test tag; проверить разрешение ресурсов сборкой Android-тестов.
- [x] 1.2 Передать существующий номер проходки и общее количество в активное содержимое стола и отобразить формат `1/2` сверху по центру; проверить Compose UI-тестами круглый и pill-стол.
- [x] 1.3 Скрывать индикатор у свободного и завершённого стола, сохраняя его у работающего и просроченного; проверить Compose UI-тестами все четыре состояния.
- [x] 1.4 Дополнить семантическое описание текущим и общим номером проходки; проверить Compose UI-тестом доступное описание активного и просроченного стола.

## 2. Переходы и итоговая проверка

- [x] 2.1 Проверить unit-тестом вычисление номера первой, промежуточной и последней проходки без нового сохраняемого состояния.
- [x] 2.2 Проверить Compose UI-тестом немедленное изменение индикатора после перехода на следующую проходку и его исчезновение после завершения последней.
- [x] 2.3 Запустить `./gradlew testDebugUnitTest assembleDebug lintDebug assembleDebugAndroidTest`, затем `./gradlew connectedDebugAndroidTest` на планшетном эмуляторе и устранить относящиеся к change ошибки.
