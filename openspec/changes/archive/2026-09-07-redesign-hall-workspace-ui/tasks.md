## 1. Логические координаты и миграция данных

- [x] 1.1 Добавить `CanvasPosition` и чистые преобразования world/viewport с проверкой граничных, нечисловых и разных масштабов в unit-тестах координат.
- [x] 1.2 Обновить модель стола и repository-контракт на логические координаты, сохранив семантику добавления и перемещения; проверить unit-тестами ViewModel и in-memory repository.
- [x] 1.3 Повысить Room schema до версии 2, добавить `canvas_x/canvas_y` и миграцию из нормализованных координат; проверить `MigrationTestHelper`, что круглые и pill-столы, проходки и таймеры не теряются.
- [x] 1.4 Перевести Room mapper/DAO/repository на canvas-координаты и проверить DAO/reopen instrumentation-тестами сохранение позиций после перезапуска базы.

## 2. Viewport, границы и сетка canvas

- [x] 2.1 Реализовать вычисление content bounds по крайним столам, минимальному viewport и рабочему запасу; проверить unit-тестами пустой, компактный, большой и отрицательно смещённый зал.
- [x] 2.2 Реализовать сохраняемое в живой сессии состояние scale/offset и ограничение pan/zoom в диапазоне `0.35x..2.5x`; проверить unit-тестами сохранение фокуса масштабирования и достижимость крайних столов.
- [x] 2.3 Заменить нормализованное позиционирование в `HallField` на world-transform и централизованное различение tap, drag стола, pan и pinch; проверить Compose UI-тестами, что жесты не запускают лишний таймер и не двигают соседние столы.
- [x] 2.4 Нарисовать minor/major-сетку в draw-фазе и скрывать minor-линии ниже порога масштаба; проверить чистую функцию выбора шага unit-тестом и наличие grid semantics в UI-тесте.

## 3. Компактное управление залом

- [x] 3.1 Удалить header «Зал» и заменить текстовые действия вертикальной группой floating-кнопок с Material-иконками для рабочего и редакторского режимов; проверить набор, test tags и безопасное размещение Compose UI-тестами.
- [x] 3.2 Перестроить контейнер `HallTableItem`, чтобы кнопка удаления полностью находилась снаружи формы и слегка перекрывала верхний правый край; проверить круглую и pill-формы UI-тестами и визуально в preview.
- [x] 3.3 Сохранить существующие состояния таймера, индикатор проходки и прямой переход одним нажатием после изменения жестов; проверить текущими unit- и HallScreen instrumentation-тестами.

## 4. Адаптивные настройки стола

- [x] 4.1 Перевести маршрут настроек на dialog/overlay destination, сохранив зал и viewport под ним и lifecycle черновика; проверить открытие, отмену, сохранение и системный Back тестами `HookahTimerAppTest`.
- [x] 4.2 Реализовать правую панель при ширине `>= 600 dp` и нижнюю панель при меньшей ширине с fixed Save/Cancel и safe/IME insets; проверить оба размера в Compose UI-тестах.
- [x] 4.3 Сделать проходки компактными строками «Проходка N» + длительность слева и удаление справа, открывая редактор длительности по нажатию на строку; проверить добавление, редактирование, удаление и перенумерацию тестами `TableSettingsScreenTest`.
- [x] 4.4 Переименовать пользовательскую строку формы в «Пилюля» и проверить выбор `PILL` без изменения доменного значения unit/UI-тестами.
- [x] 4.5 Проверить восстановление открытой панели, черновика и активного редактора при повороте и переходе через порог 600 dp; обновить recreation-тесты и убедиться, что клавиатура не перекрывает поле.
- [x] 4.6 Заменить модальный редактор названия встроенным `OutlinedTextField` панели, закрывать клавиатуру нажатием на свободную область без закрытия настроек и проверить inline-ввод, валидацию и восстановление черновика Compose UI-тестами.
- [x] 4.7 Заменить platform `dialog` настроек однооконным Compose overlay поверх постоянно скомпонованного `HallScreen`, удалить обходное управление fullscreen окна диалога и проверить сохранение immersive-режима при открытии, сохранении, отмене и системном Back на API 29/35+.

## 5. Общая проверка

- [x] 5.1 Выполнить `testDebugUnitTest`, `assembleDebug`, `lintDebug` и `assembleDebugAndroidTest`; исправить все ошибки и сохранить успешный результат команд.
- [x] 5.2 Выполнить полный `connectedDebugAndroidTest` на эмуляторе планшета и отдельные адаптивные тесты на компактном профиле; убедиться, что все тесты проходят без зависаний и падений.
- [x] 5.3 Вручную проверить на реальном планшете обе ориентации, pan/zoom, крайние столы, внешнее удаление, оба набора floating-кнопок и правую панель настроек; зафиксировать успешную проверку перед коммитом.
