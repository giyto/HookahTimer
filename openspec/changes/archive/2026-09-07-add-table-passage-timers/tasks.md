## 1. Доменная модель и время

- [x] 1.1 Добавить Room, Room KTX, Room Testing и KSP через version catalog, включить экспорт JSON-схемы базы; проверить Gradle Sync, наличие каталога схем и успешный `./gradlew assembleDebug`.
- [x] 1.2 Добавить к `HallTable` валидное рабочее состояние `Idle`, `Running(passageId, endsAtEpochMillis)` и `Completed`, включая защиту от пустого passage UUID и некорректного времени; проверить unit-тестами создание всех состояний и отклонение недопустимых значений.
- [x] 1.3 Реализовать чистую машину переходов `Idle → Running → следующая Running/Completed → Idle` с внедряемым текущим временем; проверить unit-тестами первую проходку, досрочное переключение без переноса остатка, переход после просрочки, последнюю проходку и сброс.
- [x] 1.4 Реализовать производную presentation-модель активного/просроченного состояния и форматирование `30:00`, красного `00:00`, `-00:01` и длительностей больше 59 минут; проверить unit-тестами границы секунды, точку окончания и большое значение минут.

## 2. Room и repository

- [x] 2.1 Создать сущности стола, упорядоченной проходки и метаданных зала, relation-модель и двусторонние mapper-функции; проверить unit-тестами сохранение UUID, формы, координат, порядка проходок и каждого рабочего состояния.
- [x] 2.2 Создать Room database версии 1 и DAO с `Flow` чтения, транзакционной заменой проходок и каскадным удалением; проверить instrumented-тестами пустую базу, устойчивый порядок, отсутствие дубликатов и удаление дочерних записей.
- [x] 2.3 Создать `HallRepository` и Room-реализацию для добавления, перемещения, настройки и удаления столов вместе с постоянным `nextTableNumber`; проверить repository-тестами восстановление всех данных и отсутствие повторных автоматических названий после удаления и перезапуска.
- [x] 2.4 Реализовать атомарный repository-переход таймера по актуальному состоянию стола и безопасное исправление ссылки на отсутствующую проходку; проверить instrumented-тестами быстрые последовательные переходы, независимость столов и сброс только повреждённого состояния.
- [x] 2.5 Добавить instrumented-тест файловой Room-базы: записать активный, просроченный и завершённый стол, закрыть базу и открыть заново; проверить восстановление точных `passageId`, `endsAtEpochMillis` и статусов.

## 3. Интеграция с MVVM/UDF

- [x] 3.1 Создать singleton database/repository в `Application` и фабрику `HallViewModel` без DI-фреймворка; проверить тестовой конфигурацией, что Activity и маршруты используют один repository.
- [x] 3.2 Перевести сохранённые столы `HallViewModel` на lifecycle-aware поток repository, оставив editor-состояние временным; проверить unit- и recreation-тестами первоначальную загрузку, один экземпляр каждого стола и сохранение данных при повороте.
- [x] 3.3 Перевести добавление, сохранение настроек и подтверждённое удаление на suspend-команды repository; проверить тестами, что запреты рабочего/редакторского режимов сохраняются, настройки применяются атомарно, а удаление убирает активный таймер.
- [x] 3.4 Разделить drag-preview и одно сохранение итоговой нормализованной позиции после отпускания; проверить Compose UI Test плавного перемещения внутри границ и fake-repository тестом ровно одну запись позиции на завершённый drag.
- [x] 3.5 Добавить последовательную обработку нажатий рабочего режима и временную защиту конкретного стола от повторной команды до окончания транзакции; проверить coroutine-тестами отсутствие потерянных/дублированных переходов и независимое управление несколькими столами.
- [x] 3.6 Запретить открытие настроек для `Running`/просроченного и `Completed`, не запрещая drag и подтверждённое удаление; проверить Compose UI Tests для каждого рабочего состояния и открытие настроек после сброса в `Idle`.

## 4. Планшетное отображение таймеров

- [x] 4.1 Реализовать один lifecycle-aware ticker экрана зала с внедряемым `TimeProvider`, который сразу обновляет `now` при возврате из фона и не пишет секунды в repository; проверить coroutine/UI-тестом запуск, остановку и повторный расчёт без реального ожидания минут.
- [x] 4.2 Обновить круглый и pill `HallTableItem`: `Idle` показывает название по центру, `Running` — таймер по центру и уменьшенное название снизу, просрочка — красную заливку, `Completed` — крестик без таймера; проверить screenshot/Compose UI Tests всех состояний и обеих форм в тёмной теме.
- [x] 4.3 Подключить tap-жест рабочего режима к полному циклу всех настроенных проходок и оставить редакторский tap без запуска таймера; проверить Compose UI Test последовательности из двух проходок, досрочного перехода, завершения и отдельного сброса.
- [x] 4.4 Добавить semantics с названием, номером проходки, оставшимся/отрицательным временем и завершением; проверить Compose UI Tests, что каждое состояние различимо без анализа цвета и крестика.
- [x] 4.5 Добавить интеграционные UI-сценарии двух одновременно запущенных столов, ухода/возврата приложения до и после нуля и восстановления сохранённого зала; проверить их управляемыми часами и тестовой Room-базой.

## 5. Итоговая проверка

- [x] 5.1 Запустить `./gradlew testDebugUnitTest assembleDebug lintDebug assembleDebugAndroidTest` и устранить относящиеся к change ошибки и предупреждения; проверить успешное завершение всех задач и наличие экспортированной схемы Room версии 1.
- [x] 5.2 На планшетном эмуляторе запустить `./gradlew connectedDebugAndroidTest`, затем через force-stop/повторный запуск проверить сохранение активного и просроченного таймера; зафиксировать успешные тесты и восстановление по абсолютному времени.
- [x] 5.3 На реальном планшете вручную проверить независимые столы, все переходы проходок, просрочку, крестик, сброс, drag активного стола, запрет его настройки и восстановление после закрытия приложения и перезапуска устройства; подтвердить отсутствие звуков и уведомлений.
