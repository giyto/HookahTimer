## 1. Геометрия canvas

- [x] 1.1 Добавить чистые преобразования центра viewport в мировую позицию и поиск ближайшего свободного места для нового стола; проверить unit-тестами свободный, занятый и переполненный центр.
- [x] 1.2 Уточнить расчёт content bounds и clamp для пустого, малого и большого зала; проверить unit-тестами доступность крайних столов, отсутствие ухода в пустоту и обе оси независимо.
- [x] 1.3 Проверить сохранение focal point при zoom и адаптивную детализацию сетки на граничных масштабах в `HallCanvasGeometryTest`.

## 2. Добавление стола в текущую область

- [x] 2.1 Передать рассчитанную мировую позицию через `HallAction`, `HallViewModel` и реализации `HallRepository` без изменения схемы Room; проверить существующими и новыми unit-тестами ViewModel/репозитория.
- [x] 2.2 Вычислять позицию добавления из актуального размера и transform `HallField`; проверить Compose UI-тестом, что после pan/zoom новый стол полностью виден около центра текущего viewport.

## 3. Блокировка viewport и плавающие действия

- [x] 3.1 Добавить saveable-состояние блокировки viewport и запрет обновления pan/zoom при блокировке; проверить Compose UI-тестами неизменность transform и сохранение обычного нажатия на стол.
- [x] 3.2 Добавить необходимые иконки, строки, accessibility-описания и test tags для меню, блокировки и её состояния; проверить тестами ресурсов/иконок и семантики.
- [x] 3.3 Заменить набор постоянных FAB одним раскрывающимся speed dial: три действия в рабочем режиме и два в режиме редактирования; проверить Compose UI-тестами состав действий, выполнение одного события и закрытие меню.

## 4. Фирменная визуальная тема

- [x] 4.1 Заменить стандартную фиолетовую палитру на фиксированную графитово-пастельно-зелёную Material 3 палитру и проверить обе Compose preview/основные поверхности на читаемые пары цветов.
- [x] 4.2 Сделать canvas чёрным, сетку серо-графитовой с отдельными цветами малых и крупных линий и сохранить скрытие малых клеток при уменьшении; проверить unit-тест порога и UI-семантику сетки.
- [x] 4.3 Добавить текущий логотип приложения как временный overlay справа сверху внутри safe-area с test tag/content description; проверить Compose UI-тестом отображение на планшетном размере.

## 5. Интеграционная проверка

- [x] 5.1 Обновить затронутые существующие unit- и Compose UI-тесты без ослабления проверок и выполнить `testDebugUnitTest`, `assembleDebug`, `lintDebug` и `assembleDebugAndroidTest` до успешного завершения.
- [x] 5.2 Выполнить полный `connectedDebugAndroidTest` на планшетном AVD, затем вручную проверить pan/zoom, блокировку, добавление по центру, speed dial, логотип и границы малого/большого зала на реальном планшете.
