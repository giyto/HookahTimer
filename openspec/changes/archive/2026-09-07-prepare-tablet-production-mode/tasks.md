## 1. Управление окном и полноэкранным режимом

- [x] 1.1 Создать тестируемый `TabletWindowController` с операциями удержания экрана, применения полноэкранного режима и восстановления системных панелей; проверить unit-тестом последовательности входа, выхода и повторного применения без лишних вызовов.
- [x] 1.2 Реализовать Android-контроллер через флаг удержания экрана и совместимый контроллер system bars с transient swipe; проверить инструментальным тестом установку и обязательную очистку состояния окна.
- [x] 1.3 Подключить контроллер к текущему маршруту и lifecycle приложения так, чтобы зал удерживал экран, а настройки и фон освобождали его и показывали панели; проверить fake-контроллером переходы hall → settings → hall и foreground → background → foreground.
- [x] 1.4 Добавить в `HallUiState` сессионный признак полного экрана и UDF-действие переключения, не сохраняя его в Room; проверить unit- и recreation-тестами изменение только UI-состояния и восстановление выбора после поворота в живом процессе.
- [x] 1.5 Добавить на экран зала доступное действие входа/выхода из полного экрана с правильной подписью и touch-target; проверить Compose UI Test обоих состояний, временного показа панелей в настройках и возврата на зал.

## 2. Прямой цикл действий стола

- [x] 2.1 Удалить `pendingTimerConfirmation`, типы и UDF-события подтверждения из состояния зала; проверить компиляцией и unit-тестами отсутствие ожидающего состояния после нажатия.
- [x] 2.2 Направить tap по `Idle`, непросроченному и просроченному `Running`, последней проходке и `Completed` сразу в атомарную команду перехода с текущим временем, сохранив защиту команды в полёте; проверить unit-тестами полный цикл и отсутствие двойного перехода от повторного касания.
- [x] 2.3 Удалить диалоги, строки и semantics подтверждения из экрана зала и обновить Compose UI Tests; проверить последовательными нажатиями переход между проходками, завершение и немедленный сброс крестика без появления диалога.

## 3. Адаптация планшетного интерфейса

- [x] 3.1 Перевести экран зала, панели действий и диалоги на динамические `safeDrawing` inset-ы без фиксированной компенсации status/navigation bars; проверить Compose UI Tests видимости и кликабельности крайних действий при показанных и скрытых системных панелях.
- [x] 3.2 Открывать название и длительность проходки в отдельном модальном редакторе одного поля с учётом IME, не сдвигая основной экран настроек; проверить Compose UI Test редактирования названия, первой и последней проходки и закрытия редактора.
- [x] 3.3 Реализовать компактное и расширенное расположение панели зала для планшетных окон от 600 dp в портретной и альбомной ориентации; проверить preview/Compose UI Tests минимальной поддерживаемой ширины и целевого большого планшета.
- [x] 3.4 Сохранять нормализованные координаты неизменными при resize/повороте и заново ограничивать только пиксельное отображение по доступному полю; проверить unit/UI-тестами круг и pill у всех четырёх границ без дополнительных repository-записей.
- [x] 3.5 Добавить управляемый UI-сценарий 30 столов со смесью свободных, активных, просроченных и завершённых состояний; проверить одним ticker отсутствие дубликатов, независимость действий, корректное обновление времени и восстановление после пересоздания Activity.

## 4. Итоговая проверка на планшете

- [x] 4.1 Запустить `./gradlew testDebugUnitTest assembleDebug lintDebug assembleDebugAndroidTest` и устранить относящиеся к change ошибки и предупреждения; проверить успешное завершение всех задач без изменения схемы Room.
- [x] 4.2 На планшетном эмуляторе запустить `./gradlew connectedDebugAndroidTest`, проверить прямой цикл без подтверждений, portrait/landscape, фон/возврат, system bars, настройки с IME и зал из 30 столов; зафиксировать успешное завершение всех автоматизированных сценариев.
- [x] 4.3 На реальном планшете вручную проверить удержание экрана, вход/выход из full screen, системный жест панелей, прямой переход активной проходки, немедленный сброс крестика, поворот и длительную работу большого зала; подтвердить возврат системного тайм-аута после выхода и отсутствие звуков/уведомлений.
