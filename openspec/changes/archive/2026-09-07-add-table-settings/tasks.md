## 1. Модель и интеграция состояния

- [x] 1.1 Добавить Navigation Compose в version catalog и модуль `app`; проверить успешную Gradle Sync и выполнение `./gradlew assembleDebug`.
- [x] 1.2 Создать модель конфигурации проходки со стабильным UUID и положительной длительностью в минутах, а также фабрику двух проходок по 30 минут; проверить unit-тестами значения по умолчанию, уникальность идентификаторов и отклонение некорректной длительности.
- [x] 1.3 Расширить `HallTable` упорядоченным списком проходок и подключить конфигурацию по умолчанию при добавлении стола; проверить тестами `HallViewModel`, что каждый новый стол круглый и получает две независимые проходки по 30 минут.
- [x] 1.4 Добавить в `HallViewModel` атомарное обновление названия, формы и проходок выбранного стола; проверить unit-тестами обновление только существующего выбранного стола, сохранение его позиции и отсутствие изменений при недопустимом действии.

## 2. Состояние формы настроек

- [x] 2.1 Реализовать `TableSettingsUiState`, события и `TableSettingsViewModel`, инициализирующие отдельный черновик из выбранного `HallTable`; проверить unit-тестом точное копирование названия, формы, порядка, UUID и длительностей.
- [x] 2.2 Реализовать изменение названия и формы только внутри черновика; проверить unit-тестами, что исходный `HallTable` не меняется до сохранения.
- [x] 2.3 Реализовать добавление проходки с UUID и значением `30`, удаление при наличии более одного элемента и последовательную перенумерацию в UI; проверить unit-тестами порядок, стабильность UUID оставшихся элементов и запрет удаления последней проходки.
- [x] 2.4 Реализовать валидацию обрезанного непустого названия и положительных целых длительностей, включая переполнение `Int`, и формирование результата сохранения только при полностью корректной форме; проверить unit-тестами ошибки каждого поля, атомарность и успешный результат с разными длительностями.

## 3. Планшетный интерфейс и навигация

- [x] 3.1 Добавить корневой `NavHost` с маршрутами зала и настроек по UUID стола, сохранив `HallViewModel` на уровне Activity; проверить Compose UI Test, что настройки открываются tap-жестом только в режиме редактирования, drag не открывает их, а отсутствующий UUID безопасно возвращает в зал.
- [x] 3.2 Создать планшетный `TableSettingsScreen` на Material 3 `Scaffold` с заголовком выбранного стола, прокручиваемой формой ограниченной ширины, полем названия и выбором `CIRCLE`/`PILL`; проверить Compose UI Test исходных значений и доступности сенсорных действий через semantics.
- [x] 3.3 Добавить UI упорядоченного списка проходок с цифровым вводом минут, действиями добавления и удаления и недоступным удалением последней проходки; проверить Compose UI Tests добавления, удаления, перенумерации и ввода разных длительностей при открытой клавиатуре.
- [x] 3.4 Подключить сообщения валидации, явные сохранение и отмену, а также одинаковую отмену через системное Back; проверить Compose UI Tests, что ошибки не закрывают форму, отмена не меняет стол, а успешное сохранение обновляет название и форму в зале без запуска таймера.
- [x] 3.5 Сохранить маршрут и черновик формы при пересоздании Activity в живом процессе; проверить инструментальным тестом, что после поворота планшета восстанавливаются выбранный стол и несохранённые название, форма и длительности.

## 4. Итоговая проверка

- [x] 4.1 Запустить `./gradlew testDebugUnitTest assembleDebug lintDebug` и устранить ошибки и предупреждения, относящиеся к change; проверить успешное завершение всех задач Gradle.
- [x] 4.2 На эмуляторе планшета запустить `./gradlew connectedDebugAndroidTest`, затем вручную проверить на реальном планшете открытие настроек, ввод с экранной клавиатуры, сохранение, отмену и переключение формы; зафиксировать успешное завершение UI-тестов и ручной проверки.
