## 1. Настройка проекта и архитектурного каркаса

- [ ] 1.1 Добавить в version catalog и `app` зависимости Room, KSP, Navigation Compose, ViewModel Compose и Coroutines; проверить синхронизацию и сборку командой `gradlew.bat assembleDebug`.
- [ ] 1.2 Создать пакеты `data`, `domain`, `presentation` и `ui`, настроить объект приложения и ручную фабрику зависимостей; проверить, что приложение запускается без изменения пользовательского поведения.
- [ ] 1.3 Настроить Navigation Compose с маршрутами зала и создания/редактирования стола; проверить переходы простым Compose navigation-тестом.

## 2. Доменные модели и машина состояний

- [ ] 2.1 Реализовать доменные модели стола, формы, проходки и состояний `IDLE`, `RUNNING`, `COMPLETED`, включая индекс проходки и deadline; проверить создание корректных моделей unit-тестами.
- [ ] 2.2 Добавить внедряемый `Clock` и функции расчёта оставшегося времени, просрочки и отображаемой фазы; проверить границы до нуля, в ноль и после нуля unit-тестами с фиксированным временем.
- [ ] 2.3 Реализовать чистую машину переходов `IDLE → RUNNING → следующая RUNNING → COMPLETED → IDLE`; проверить обычные, досрочные, просроченные и однопроходные циклы параметризованными unit-тестами.
- [ ] 2.4 Реализовать валидацию непустого названия, минимум одной проходки и положительной целой длительности в минутах; проверить все допустимые и ошибочные варианты unit-тестами.

## 3. Хранение данных в Room

- [ ] 3.1 Создать `TableEntity`, `RoundEntity`, связи, converters, DAO и базу версии 1 с каскадным удалением; проверить схему и CRUD на in-memory Room в инструментальных тестах.
- [ ] 3.2 Реализовать преобразования между Room-агрегатами и доменными моделями с сохранением порядка проходок; проверить round-trip преобразование unit-тестами.
- [ ] 3.3 Реализовать repository с `Flow` списка столов и операциями создания, настройки, перемещения и удаления; проверить наблюдение изменений и значения по умолчанию тестами repository.
- [ ] 3.4 Выполнять переход состояния, обновление индекса и deadline одной транзакцией; проверить согласованность данных после каждого типа перехода инструментальными тестами.
- [ ] 3.5 Блокировать сохранение конфигурации для `RUNNING`, не затрагивая текущий таймер; проверить отказ и неизменность данных тестом repository.

## 4. Presentation-слой и таймеры

- [ ] 4.1 Реализовать `HallViewModel` с `StateFlow<HallUiState>`, наблюдением repository и обработчиками нажатия, перемещения и удаления; проверить однонаправленные обновления состояния unit-тестами.
- [ ] 4.2 Добавить секундный UI-тик, который пересчитывает отображаемое время из `deadline` и `Clock`, но не записывает посекундные изменения в Room; проверить несколько последовательных значений виртуальным временем coroutine-теста.
- [ ] 4.3 Реализовать независимый расчёт состояния для нескольких столов; проверить разные deadline и отсутствие взаимного влияния unit-тестом ViewModel.
- [ ] 4.4 Реализовать состояние и события экрана настройки с черновиком, значениями по умолчанию и ошибками валидации; проверить создание и редактирование unit-тестами.

## 5. Экран зала и Composable стола

- [ ] 5.1 Заменить шаблонный `Greeting` экраном зала на Material 3 с `Box`, пустым состоянием и действием добавления; проверить оба состояния Compose UI Tests.
- [ ] 5.2 Реализовать отдельный Composable стола для круглой и pill-формы с нейтральной, активной, просроченной и завершённой визуализацией; проверить форму, цвет, таймер, положение названия и крестик screenshot/Compose UI Tests.
- [ ] 5.3 Добавить форматирование положительного и отрицательного времени и обновление текста на экране; проверить формат на границах минут и часов unit-тестами.
- [ ] 5.4 Реализовать перевод нормализованных координат в позицию внутри `Box` с ограничением по видимым границам; проверить крайние координаты unit-тестами и Compose UI Test.
- [ ] 5.5 Реализовать распознавание tap и drag через touch slop, локальное плавное перемещение и сохранение позиции после отпускания; проверить, что drag не переключает состояние, а tap не меняет позицию, Compose UI Tests.
- [ ] 5.6 Добавить стабильные semantics для id, названия и состояния каждого стола; проверить поиск и взаимодействие со столами в Compose UI Tests.

## 6. Экран настройки и безопасные действия

- [ ] 6.1 Реализовать форму названия и переключатель круглой/pill-формы с отображением ошибок; проверить сохранение допустимого значения и отказ для пустого названия Compose UI Tests.
- [ ] 6.2 Реализовать упорядоченный список проходок с добавлением, удалением и отдельной длительностью в минутах; проверить две проходки по 30 минут по умолчанию и запрет удаления последней Compose UI Tests.
- [ ] 6.3 Показать режим только для просмотра и объяснение запрета сохранения для активного или просроченного стола; проверить отсутствие записи изменений Compose UI Test.
- [ ] 6.4 Добавить диалог подтверждения удаления и возврат в зал после подтверждения; проверить сценарии подтверждения и отмены Compose UI Tests.

## 7. Восстановление и интеграция жизненного цикла

- [ ] 7.1 Подключить базу и repository к реальному графу приложения и восстановить зал после пересоздания Activity; проверить конфигурацию, формы и координаты инструментальным тестом.
- [ ] 7.2 Восстанавливать активный таймер по абсолютному deadline до и после его окончания; проверить оставшееся время и немедленную просрочку тестами с контролируемыми часами.
- [ ] 7.3 Ограничить секундный тик временем активного отображения экрана, сохраняя корректный пересчёт после возврата из фона; проверить сценарий background/foreground lifecycle-тестом.
- [ ] 7.4 Проверить сохранение `IDLE`, `RUNNING` и `COMPLETED` после закрытия и повторного запуска тестового процесса или эквивалентным end-to-end инструментальным сценарием.

## 8. Итоговая проверка MVP

- [ ] 8.1 Запустить `gradlew.bat testDebugUnitTest` и исправить все unit-тесты до успешного результата.
- [ ] 8.2 Запустить `gradlew.bat connectedDebugAndroidTest` на планшетном эмуляторе API 26 и актуального API; убедиться, что все Room и Compose UI Tests проходят.
- [ ] 8.3 Запустить `gradlew.bat lintDebug assembleDebug` и устранить ошибки сборки и lint, относящиеся к изменению.
- [ ] 8.4 Вручную пройти на планшетном эмуляторе цикл нескольких столов от создания до просрочки, завершения, сброса и перезапуска приложения; зафиксировать успешный чек-лист приёмки по всем четырём спецификациям.
