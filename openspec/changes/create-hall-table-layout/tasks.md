## 1. Основа состояния зала

- [x] 1.1 Добавить в version catalog и модуль `app` только зависимости Lifecycle ViewModel/Compose и сбора StateFlow с учётом lifecycle; проверить успешную Gradle Sync и выполнение `./gradlew assembleDebug`.
- [x] 1.2 Создать модели стола, формы и нормализованной позиции, включая чистые функции преобразования и ограничения координат; проверить unit-тестами значения внутри границ, за каждым краем и при поле меньше стола.
- [x] 1.3 Реализовать `HallUiState`, события и `HallViewModel` для режима редактирования, добавления, изменения позиции и запроса удаления; проверить unit-тестами уникальные UUID, последовательные названия `Стол N` и неизменность схемы от запрещённых действий рабочего режима.
- [x] 1.4 Реализовать подтверждение и отмену удаления в `HallViewModel`; проверить unit-тестами, что удаляется только выбранный стол и только после подтверждения.

## 2. Интерфейс рабочего поля

- [x] 2.1 Создать `HallScreen` с Material 3 `Scaffold`, рабочим полем `Box`, пустым состоянием и явным переключателем режима редактирования; проверить Compose UI Test для пустого зала и отображения признака активного режима.
- [x] 2.2 Создать отдельный Composable стола с центрированным названием и вариантами `CIRCLE` и `PILL`; проверить Compose UI Tests или screenshot-preview, что обе формы и названия доступны через UI semantics.
- [x] 2.3 Подключить добавление стола только в режиме редактирования и безопасное начальное размещение внутри измеренного рабочего поля; проверить Compose UI Test, что действие скрыто в рабочем режиме, а добавленный стол виден целиком.
- [x] 2.4 Реализовать drag-жест с порогом движения, мгновенным обновлением нормализованной позиции и ограничением по размерам поля и стола; проверить Compose UI Tests, что drag работает только в режиме редактирования, обычный tap не перемещает стол и выход за края невозможен.
- [x] 2.5 Добавить отдельное действие удаления и Material 3 confirmation dialog; проверить Compose UI Tests для отмены, подтверждения и недоступности удаления в рабочем режиме.
- [x] 2.6 Заменить шаблонный `Greeting` в `MainActivity` экраном зала, получить `HallViewModel` на уровне Activity и собирать состояние с учётом lifecycle; проверить вручную на эмуляторе, что столы и позиции сохраняются после поворота устройства в рамках живого процесса.

## 3. Итоговая проверка

- [x] 3.1 Удалить шаблонные тесты и дополнить unit/UI-набор всеми сценариями `hall-layout`; проверить выполнение `./gradlew testDebugUnitTest` и отсутствие непроверенных переходов ViewModel.
- [x] 3.2 Запустить `./gradlew assembleDebug lintDebug` и `./gradlew connectedDebugAndroidTest` на эмуляторе планшета; проверить успешное завершение сборки, lint и UI-тестов и вручную убедиться, что столы удобно добавлять, двигать и удалять касанием.
